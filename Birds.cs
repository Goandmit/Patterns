namespace Patterns
{
    public class Birds : Animal
    {
        public Birds(string className, string animalType, string name, string foodType, string livingEnvironment)
            : base(className, animalType, name, foodType, livingEnvironment) { }
    }
}
